# modpack/overrides/

Files copied verbatim into the instance root on top of the base pack
(the `overrides/` concept of an `.mrpack`). Examples: a default `servers.dat`
pointing at our server, `options.txt` defaults, launcher icons.

Never put secrets, `eula.txt`, worlds, or player data here. Empty for now.
